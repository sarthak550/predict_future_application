# Build output
.next
dist
coverage
*.tsbuildinfo

# Dependencies
node_modules

# Environment (never commit secrets)
.env
.env.local
.env.*.local
.env.production
.env.prod
.env.dev

# Turbo
.turbo

# Prisma local DB
prisma/dev.db
prisma/dev.db-journal

# Expo
.expo

# OS
*.DS_Store
Thumbs.db

# Debug
*.log
npm-debug.*

# Local backup files (manual snapshots, pre-pull copies, device backups)
*.pre-*-backup
*.pre-pull-backup
*.backup
expo_go_backup.ab

# DB dump backups written by operational scripts (e.g. scripts/reset-markets.ts) —
# may contain user data (usernames, reasoning text, wallet amounts); never commit.
apps/api/backups/
