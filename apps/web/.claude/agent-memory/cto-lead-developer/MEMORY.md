# CTO Memory Index (apps/web)

- [Dashboard bug-trio audit — SHIPPED AS-IS (2026-08-05)](project_dashboard_bug_trio_2026_08_05.md) — killed-mid-work session's partial diff (symbol-persistence, live LTP, P&L-colored position lines across all terminal surfaces) audited hunk-by-hunk, found already complete/correct, zero new code needed. tsc/eslint/ta:check(514) all clean.
- [Backtest Metrics Engine (2026-08-04)](project_backtest_metrics_engine.md) — TradingView-grade stats added to lib/ta/backtest.ts only (UI ticket deferred); SIGN/NULL/OPEN-TRADE laws + annualization 90-day honesty floor; ta:check 195→514.
- [Futures contract table in maximized workbench (2026-08-09)](project_workbench_futures_contracts_tab.md) — [Ticket|Contracts|Strategy] 3rd tab via new `chainLabel` prop; underlying-switch is live-chart, contract-month-switch is ticket-only (chart keys on underlying not contract); same auto-restore no-op fix as options; ta:check baseline now 164 (concurrent sprint drift, not from this change).
- [Option chain in maximized workbench (2026-08-08)](project_workbench_option_chain_tab.md) — [Ticket|Chain|Strategy] 3rd tab, live premium contract-switch (no remount), no-op'd a dead-code auto-close that this feature made reachable, First-Load verified byte-identical via stash-rebuild-compare.
- [TA Suite Sprint S1 — drawings+toolbar+style editor](project_ta_suite_s1.md) — all T1-T7 built 2026-08-02, code-complete, tsc/eslint/build/engine clean. Load-bearing klinecharts internals: figure-style merge order, path-figure x/y offset quirk, performEvent mutation contract, simpleAnnotation extendData-as-function fix, brush's open color/size. Runtime QA pass still pending (no dev server in this session).
- [TA Suite Sprint S2 — indicator library + settings](project_ta_suite_s2.md) — T1-T5 built 2026-08-02, code-complete, all gates clean (264/264 engine, bundle unchanged). 27 built-ins + 14 customs, multi-instance model, id-filtered create/remove/override needs NO paneId tracking (verified simplification), RSI/STOCHRSI bugs caught by hand-fixture tests and fixed. S3 strategies reuse lib/ta/math.ts as-is.
- [TA Suite Sprint S3 — strategies+backtest+PF_SIGNALS+tabs](project_ta_suite_s3.md) — T1-T5 built 2026-08-02, code-complete, all gates clean incl. ta:check (59/59, negative-case verified) and engine 264/264. Closes the 3-sprint TA Suite program. WMA/VWMA/HMA moved to main pane (orchestrator correction); PF_SIGNALS non-interactivity is structural (draw()-only, no hit-test path exists); disclaimer needed its own DOM position outside the tab-CSS-hide.
- [TA Suite founder-feedback pass (2026-08-03)](project_ta_suite_founder_feedback_2026_08_03.md) — flyout-nested-button-bubbling bug fixed (button+flyout must be SIBLINGS, not parent/child); 25 more tools (62→87 real overlays), new Cycles family. All gates green, bundle unchanged.
- [TA Suite legends + crosshair-follow pass (2026-08-04)](project_ta_suite_legends_founder_feedback.md) — on-chart indicator legends are klinecharts-NATIVE (figures[].title, already free); added eye/gear/remove via TooltipFeatureStyle (real click path, verified); crosshair payload has no dataIndex (re-derive via convertFromPixel); overrideIndicator styles.lines[] REPLACES not merges (modulo-indexed); abcd/xabcd ratio labels; flyout viewport-fit clamp.
- [TA Suite stats-pills pass (2026-08-05)](project_ta_suite_stats_pills_founder_feedback.md) — TV-parity Δ/%/bars, price/date pin, channel-width stats on 11 klinecharts BUILT-INS via verified same-name registerOverlay override (confirmed: plain object-key assignment, always overrides); selection tracked via onSelected/onDeselected (Set), repaint guaranteed via updatePane; native axis price/date tag already covers "selected" for horizontal/vertical pins — only "drawing" was a real gap. Zero new overlay names, tool-registry.ts untouched.
- [Signals table + educational reasons + liveness audit (2026-08-06)](project_ta_suite_signals_panel_liveness_audit.md) — computeTechnicalDetail shares ONE rule table with computeTechnicalRating (ta:check now asserts tally equality, 86/86); 3 shared reason-text builders reused by indicator-signals.ts where conventions overlap, 7 indicators get bespoke text; SignalReasonTrigger hover/tap card reused by table+strip. Liveness audit found NO broken link end-to-end (verified against klinecharts dist source directly) — only fix was 60s->30s poll cadence for 1m/5m + new HeartbeatChip.
- [Custom signals section + builder](project_ta_suite_custom_signals.md) — 3rd "Custom" section in signals table; 11 oscillator rules refactored into generic evaluate*Rule()+wrapper so CUSTOMIZABLE_RULES (13 entries) reuses the SAME evaluator as the standard table (provable no-drift, not just docs); custom-signal-builder.tsx owns pf.workbench.customSignals + the "+"/edit popover; ta:check 120/120; rating dial untouched by design.
- [Naming-disambiguation audit (2026-08-04)](project_ta_suite_naming_disambiguation_audit.md) — "Stochastic" was 3 DIFFERENT formulas (KDJ recursive+J-line vs classic SMA-smoothed %K/%D vs StochRSI), verified against klinecharts source, now named distinctly everywhere; 33/41 indicators got displayLabel; found+fixed a real bug (formatInstanceLabel showed raw key not displayLabel) + dialog sort-by-key gap. ta:check 124/124, bundle unchanged.
- [Per-line indicator color fix (2026-08-04)](project_ta_suite_per_line_indicator_color_fix.md) — fixed founder bug "all lines same color": Style section now discovers live figures via chart.getIndicators({id}).figures (no catalogue), builds a DENSE per-line styles.lines[] (formatValue is all-or-nothing, verified), persists per-instance overrides additively in v2 localStorage. Width stays shared (CTO call). SUPERTREND color override is a verified no-op (flip-color callback wins).
- [User Strategy Scripting Sprint SS1 (2026-08-04)](project_scripting_ss1.md) — schema+CRUD+lib/ta/user-scripts.ts+strategy-worker.ts (first Web Worker)+PF_SIGNALS precomputed mode, ta:check 124→164. V8 has NO usable SyntaxError line info from new Function/eval (use acorn pre-parse instead); schema-pair "verified ground truth" was wrong (apps/web's copy is orphaned, never codegen'd); chart-workbench.tsx hard-constraint collision resolved via legacy-shape normalization, zero effect-body lines changed.
- [User Strategy Scripting Sprint SS2 (2026-08-04)](project_scripting_ss2.md) — CodeMirror editor drawer UI, ta:check 182→192. CodeMirror+lang-javascript floor measured at ~150KB gz (basicSetup ~165KB) — the brief's 80KB target is not achievable, flagged not silently missed. First Load JS +367B (~0.27%, routing-table churn from a deleted route, zero app-code leakage, precisely root-caused). chart-workbench.tsx's own chunk grew only 161 bytes. Parity-fixture honesty gate mostly vacuous under default params on the 10-bar fixture (documented, verified negatively via a real mutation test instead).
- [User Strategy Scripting Sprint SS3 (2026-08-04)](project_scripting_ss3.md) — closes the program: full 13-item live-browser security matrix ALL PASS (real Playwright, real accounts), Cmd+Enter/Cmd+S shortcuts (defaultKeymap's own Mod-Enter conflict found+beaten), empty-state CTA, narrow-viewport drawer stacking, standalone-build deploy verification + live 3-terminal parity. ta:check 192→195. Bundle delta -1B (noise). Key gotchas: Playwright click-actionability breaks on this app's fixed-overlay taller-than-viewport layout (use el.click() via evaluate), CodeMirror closeBrackets mangles keyboard.type() (use clipboard paste), item 13's race needed one-time reverted source instrumentation to prove (Playwright's own worker-event tracking misses a ~1ms-lived first worker).
- [Tool-values-gap-fixes brief (2026-08-04)](project_ta_suite_tool_values_gap_fixes.md) — closed 15 TV-parity gaps (measure elapsed-time+₹risk/reward, 6 fib %labels, 3 gann grid/corner labels, cypher formula BUG fix + threeDrives ratios). KEY: figure-kit.ts is Node-safe to import into lib/ta/selfcheck.ts (only `import type` from klinecharts) — put NEW pure overlay formulas there, not in family files (which import registerOverlay as a runtime value). ta:check 164→182.
- [Quote-driven intrabar ticks (2026-08-04)](project_quote_driven_intrabar_ticks.md) — new ~4-5s fast quote poll (apps/api liveQuote.ts, 4s cache) folded honestly into the forming bar (workbench) / appended as a tick (small charts); caught+fixed a shrinking-wick bug via a commit-phase running-extremes ref; QA caught a bonds-page regression (recurring 404 poll on a non-equity symbol) fixed via a real `quoteSource={false}` opt-out — full call-site audit recorded. First-Load +1kB/page (expected, sync PriceChart). ta:check 195/195, engine 275/275 (baseline drift from unrelated concurrent WIP).
- [Scripts drawer UI fix + adjustability (2026-08-04)](project_scripting_ss4_ui_fix.md) — founder said only "needs fixing, adjustable"; diagnosed live via Playwright FIRST. Found: drawer wasn't full-width (missing w-full on a flex-row child, unstable 512-947px), and dragging the drawer taller could crush it into kline-chart.tsx's 420px floor, breaking the handle itself (elementFromPoint proved it). Fixed both + added sidebar-width/console-height drag + collapsible results, all pf.workbench.scripts.* persisted. New shared use-drag-resize.ts hook (3rd/4th handle copy avoided). Self-caught regression: console+editor min-heights conflicted until DRAWER_MIN_HEIGHT raised to the exact safe floor (320). 100% confined to the drawer chunk — zero chart-workbench.tsx/kline-chart.tsx edits.
- [Scripts drawer SS4 QA-fix pass (2026-08-05)](project_scripting_ss4_qa_fix.md) — SS4 FAILED QA on 2 bugs, both fixed same day, code/unit-verified only (no dev server this round). Bug 1: DRAWER_MIN_HEIGHT floor was overriding collision safety below ~840px viewport (QA proved live down to 700px, real dblclick timeout) — fixed by letting the drawer legitimately shrink below 320px on short viewports instead of gating behind a "too short" message; internal editor/console budget math re-derived to degrade gracefully (console force-collapses when it can't fit). Bug 2: cold-mount effect race corrupted a valid saved consoleHeight (stale closure in a `[drawerHeight]` sync effect) — fixed with a first-render guard ref. Gotcha: this repo's tsconfig makes .tsx modules import as CJS-interop under `tsx` (named exports live under `mod.KEY`, not plain named imports).
