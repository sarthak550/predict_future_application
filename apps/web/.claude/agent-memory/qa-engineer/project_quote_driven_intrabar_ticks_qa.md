---
name: project_quote_driven_intrabar_ticks_qa
description: use-workbench-candles.ts interval-race + rollover-stall fix — PASS-WITH-CONDITION 2026-08-05 (prior session interrupted, this completed the verdict).
metadata:
  type: project
---

Ticket: interval-race fix (latestUrlRef/latestPremiumKeyRef "latest request wins" guards) + rollover-stall fix (extendProvisionalBar/provisionalBarRef) in `apps/web/components/paper-trading/workbench/use-workbench-candles.ts`. A prior QA session on this exact ticket was interrupted before issuing a verdict (its live minute-boundary Playwright watch got killed) — no memory file existed for it; this session ran the full pass fresh and closed it out.

**Verdict: PASS-WITH-CONDITION.** Market was closed (22:20+ IST) — the live minute-boundary provisional-bar-creation observation is physically impossible outside market hours and was NOT faked. Outstanding condition: watch the provisional-bar behavior at a real 2026-08-06 09:15 IST market-open boundary.

What was actually verified, and how:
1. **Guard trace (full, by reading, not assuming)**: `git diff` confirmed the change is self-contained to this one file (351-line diff, verified line-by-line). `latestUrlRef`/`latestPremiumKeyRef` set synchronously before `fetch()`; every `.then` AND `.catch` on both fetch paths (equity/index `fetchOnce`, premium `fetchPremiumOnce`) checks identity before touching `setState` — confirmed the catch branches guard too (stale errors can't clobber fresh data), not just the success branches. The 30s/60s poll calls the same `fetchOnceRef.current`, so one guard covers both mount-fetch and poll.
2. **Live race repro — 3 Playwright test suites, real dev servers (apps/web :3000 + apps/api :3001, both freshly started, apps/web with `.next` wiped first), signed in as kira@example.com**:
   - Test A: 5x hard-refresh + reopen maximized workbench with `pf.workbench.interval` localStorage pre-set to "1m" (default mount state is hardcoded "5m") — real unmocked endpoints. All 5 runs: "1m" pill correctly won the race, no error banner. This alone is somewhat probabilistic (fast local server can resolve both requests before a race even manifests), so it was backed by:
   - Test B (deterministic, `page.route()`-forced worst case): the OLD/first-fired "5m" request delayed 1500ms and stubbed to return HTTP 500; the NEW/second-fired "1m" request resolves fast (200ms) with valid stub data. This is the exact "stale ERROR must not clobber fresh data" scenario named in the module's own doc comment. Result: no error banner, chip stayed "1m", canvas stayed rendered — the late 500 was correctly dropped by the `.catch` guard.
   - Test C: rapid 1m→5m→1m pill clicking — final chip correctly settled on "1m", "5m" pill not active, no error banner.
   - NOT independently live-verified: the SUCCESS/SUCCESS clobber case (old interval's fetch succeeds and resolves after the new one) — the DOM has no visible read-out of the workbench's own `quote.price` while maximized (it only reaches the DOM via `chartQuote` → holdings-table `liveLtp`, which requires an actual held position; not worth the setup cost given the guard line itself is a trivial `if (latestUrlRef.current !== url) return;` before any setState, already read verbatim and structurally identical to the proven-working catch-branch). Flagged as a known gap in method, not a failure.
3. **Provisional-bar logic — unit-level against the REAL extracted function bodies** (not hand-retyped copies): wrote a script that programmatically extracts `extendProvisionalBar`/`foldQuoteIntoCandles` verbatim from the source file via brace-matching, drops them into a standalone `.ts` module, and ran 35 assertions against fabricated ticks through `tsx` (real TS compilation, not `new Function` — TS type annotations break `new Function`/`eval`). All 35 passed: exact-boundary tick creates aligned-timestamp bar; accumulation is monotonic (high never drops, low never rises, open stays fixed, close tracks latest); volume always 0; 2-buckets-ahead → null (data-outage cap); exactly-at-second-boundary → null (confirms exclusive upper bound); 1ms-before-second-boundary → accepted. Also traced (read, not assumed) the in-place-replacement path through `kline-chart.tsx`'s data effect (`apps/web/components/paper-trading/workbench/kline-chart.tsx:1000-1024`): `lastTs !== undefined` is true whenever there's any candle, so every `displayCandles` identity change (provisional append, provisional extend, or real-bar replace) pushes through `subscribeBarCallbackRef.current`, and klinecharts' single-bar `_addData` append-or-replaces by timestamp — confirmed no flicker on handoff.
4. **Interval-switch-mid-provisional discard**: NOT an explicit clear — self-heals via timestamp-identity check (`sameProvisional = existingProvisional.timestamp === nextBarStart`). Independently re-derived this is correct in the overwhelming majority of cases (a stale finer-interval provisional's timestamp essentially never coincides with a coarser interval's new boundary) but flagged one narrow theoretical edge: if a stale provisional's timestamp happens to exactly equal the new interval's `nextBarStart` (e.g., 1m→60m switch where the provisional was created exactly on an hour boundary), the new provisional could carry forward a wrong `open`. Severity: cosmetic only (one forming bar's open field), self-corrects on the very next real fetch, never breaks a closed bar. Not a blocking finding — matches the developer's own documented "verified by tracing, not just assumed" claim, now independently re-verified.
5. **Gates**: tsc (apps/web) clean, eslint clean on the file, `ta:check` 514/0 (`apps/web/lib/ta/selfcheck.ts`), engine self-check 275/0 (`apps/api/scripts/verify-papertrading-engine.ts` — this is what "engine" refers to in this codebase; it's the paper-trading cost/replay engine self-check, unrelated to this specific file but is the standing system-health gate).

**Process notes for next time:**
- `apps/web` and `apps/api` are SEPARATE Next.js apps on :3000/:3001; the candles route is a thin proxy from web → api (`API_INTERNAL_URL`, default `127.0.0.1:3001`) — api must be running for real (unmocked) candle data to flow, even though auth/session lives entirely in apps/web's own NextAuth (`apps/web/lib/auth.ts`, own Prisma client, not a proxy to apps/api for sign-in).
- Test login: kira@example.com / Password123! via `/sign-in` (2 bare `<input>` elements, no name/id/testid — select by index 0=email, 1=password). Sign-in redirects to "/" by default (no callbackUrl).
- `/paper-trading?symbol=SYMBOL` deep-links directly to a focused instrument — avoids needing to drive the symbol-search autocomplete.
- The maximize button: `button[title="Maximize chart"]` (also has matching `aria-label`). The maximized overlay root: `[data-chart-key]`. Timeframe pills: `button` with exact text "1m"/"5m"/etc inside that overlay, active state = class contains `bg-ink-900`.
- Visiting `/paper-trading` while signed in AUTO-CREATES a `PaperTradingAccount` row for that user as a side effect of `loadAccount()` (no explicit "create account" action needed) — if you navigate there for QA and don't intend to leave persistent test state, check `paperTradingAccount.findFirst({where:{userId}})` before/after and delete it in cleanup if it was created fresh (0 orders). Kira had no account before this session; one was auto-created and deleted again after.
- To unit-test a private, non-exported helper function from a "use client" React-hooks file without hand-retyping its logic (risk of testing your own rewrite instead of the real code): extract the verbatim source text via brace-matching, write it to a scratch `.ts` module with `export` prepended, and run it through `tsx` (which handles the real TS syntax) rather than `new Function`/`eval` (breaks on TS type annotations).
- Route-based (`page.route()`) response mocking/delaying is a legitimate way to deterministically force a specific race ordering for a timing-dependent bug, rather than hoping N plain reloads reproduce it by luck on a fast local dev server — used for Test B here, worth reaching for again on similar race-condition tickets.
